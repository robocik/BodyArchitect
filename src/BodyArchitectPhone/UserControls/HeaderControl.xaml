<UserControl x:Class="BodyArchitect.WP7.UserControls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
    FontFamily="{StaticResource CustomFontFamilySemiLight}"
    FontSize="{StaticResource CustomFontSizeLarge}"
    Foreground="{StaticResource CustomForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <Border  Style="{StaticResource HeaderBorder}">
        <ContentPresenter Margin="12,0,0,0" Name="contentPresenter" VerticalAlignment="Center"/>

    </Border>

</UserControl>
